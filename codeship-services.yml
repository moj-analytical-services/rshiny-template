app:
  build:
    image: 593291632749.dkr.ecr.eu-west-1.amazonaws.com/rshiny-example
    dockerfile: Dockerfile
  cached: true

deployer:
  image: quay.io/mojanalytics/analytics-ci:v4
  encrypted_env_file: secrets.env.encrypted

pusher:
  image: docker:dind
  add_docker: true

aws_dockercfg:
  image: codeship/aws-ecr-dockercfg-generator
  add_docker: true
  encrypted_env_file: secrets.env.encrypted
